# PDF.js vendor files

This folder contains the browser build of `pdfjs-dist` pinned to version `6.0.227`.

Files:

- `pdf.mjs`
- `pdf.worker.mjs`
- `LICENSE`

The self-assessment page serves these files locally so the CV import does not require a third-party CDN request. PDF parsing still happens only in the user's browser.
